- name: variables from args    #variables from command line arguments
  hosts: local
  connection: local
  tasks:
  - name: print name and age from args
    ansible.builtin.debug:
      msg: "name: {{ name }}, age: {{ age }}"