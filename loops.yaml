- name: demo on loops
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "Hello {{ item }}" #item is reserved keyword in ansible and it will hold the current item value in the loop
    loop:
    - Anusha
    - Manikanta
    - Balaji